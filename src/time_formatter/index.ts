import type { Event } from './types';

/**
 * This function should return a string that represents the combined time spent on the specified event types
 * @param data an array of events
 * @param eventTypes an array of event types
 * @returns 
 */
export const timeFormatter = (data: Array<Event>, eventTypes: Array<string>) => {
	return '';
};
