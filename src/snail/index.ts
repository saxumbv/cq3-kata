import type { Grid, Path } from "./types";

/**
 * A snail walks over a grid. It starts at the top left corner and walks in a clockwise spiral.
 * When it hits a wall, it turns right. The snail can only walk on the grid. Give the path the snail walks.
 * See: https://www.codewars.com/kata/521c2db8ddc89b9b7a0000c1 for a visual example.
 * @param grid a grid to walk over
 * @returns the path from the snail walking over the grid
 */
export const snail = (grid: Grid): Path => {
  return [];
};
